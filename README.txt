DESCRIPTION
--------------------

INSTALL
--------------------

SETUP
--------------------

GITHUB

First step:                     git init
Connect to github repo:         git remote add origin %repo%
Adding changes to commit:       git add .
Commit changed:                 git commit -m "name or push"
Pushing changes to github:      git push -u origin main
Clone repo:                     git clone %repo%
Fetch updates from repo:        git fetch %repo%
Make sure you're on main:       git branch (has to be main), if not: git checkout -b main

Repo for AAD:                   https://github.com/rubenpost/Model_agnostic_AAD.gitgit bran